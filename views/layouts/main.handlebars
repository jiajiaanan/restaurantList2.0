<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Restaurant List</title>
  <!-- link css files here -->
  <link rel="stylesheet" href="/stylesheets/bootstrap.css">
  <link rel="stylesheet" href="/stylesheets/style.css">

  <link rel="shortcut icon"
    href="https://assets-lighthouse.s3.amazonaws.com/uploads/image/file/6227/restaurant-list-logo.png"
    type="image/x-icon">
  <!--link fontawesome icon-->
  <script src="https://kit.fontawesome.com/ca75e2c0ec.js" crossorigin="anonymous"></script>
</head>

<body>
  <header>
    <!-- navigation -->
    <nav class="navbar navbar-light bg-light">
      <div class="container d-flex align-items-center">
        <a class="navbar-brand" href="/">
          <img src="https://assets-lighthouse.s3.amazonaws.com/uploads/image/file/6227/restaurant-list-logo.png"
            width="30" height="30" class="d-inline-block align-top" alt="我的餐廳清單">
          {{#if isAuthenticated}}{{ user.name}}的 {{/if}}餐廳清單
        </a>
        {{!-- logout/login --}}
        <div class="text-end">
          {{#if isAuthenticated}}
          <a href="/users/logout" class="btn btn-major">Logout</a>

          {{else}}
          <a href="/users/login" class="btn btn-major">Login</a>
          {{/if}}
        </div>

      </div>
    </nav>
    {{#if isAuthenticated}}
    <div class="banner"></div>
    {{/if}}
  </header>





  <!-- partial templates will replace the part of "body" here -->
  {{{ body }}}

  <script src="/javascripts/popper.js"></script>
  <script src="/javascripts/bootstrap.js"></script>
</body>

</html>